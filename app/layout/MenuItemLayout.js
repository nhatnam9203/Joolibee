import React from 'react';
import { View, StyleSheet } from 'react-native';

const MenuItemLayout = () => <View style={styles.container} />;

const styles = StyleSheet.create({
  container: { flex: 1 },
});

export default MenuItemLayout;
