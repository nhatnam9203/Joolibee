import React from 'react';
import { TopBarScreenLayout } from '@layouts';
import { TopBarComponent } from '../../components';

const MenuPage = () => <TopBarScreenLayout topBar={<TopBarComponent />} />;

export default MenuPage;
